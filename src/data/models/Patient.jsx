export default class Patient {
    constructor(id, name, phone, email, medicalHistory) {
        (this.id = id),
        (this.name = name),
        (this.phone = phone),
        (this.email = email),
        (this.medicalHistory = medicalHistory);
    }
}