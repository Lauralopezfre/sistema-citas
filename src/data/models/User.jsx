export default class User {
  constructor(id, name, password, phone, email, address, date) {
    (this.id = id),
      (this.name = name),
      (this.password = password),
      (this.phone = phone),
      (this.email = email),
      (this.address = address),
      (this.date = date);
  }
}
