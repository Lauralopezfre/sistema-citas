export default class MedicalHistory {
    constructor(id, symptom, prescription, diagnosis) {
        (this.id = id),
        (this.symptom = symptom),
        (this.prescription = prescription),
        (this.diagnosis = diagnosis);
    }
  }