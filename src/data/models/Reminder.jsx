export default class Reminder {
    constructor(id, paciente, cita, phone) {
      (this.id = id),
        (this.phone = phone),
        (this.cita = cita),
        (this.paciente = paciente)
    }
  }